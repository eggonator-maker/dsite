{% if title is not empty or required %}
  <label{{ attributes.addClass('form-check-label') }}>
    {{ title }}
    {% if required %}
      <span class="required">*</span>
    {% endif %}
  </label>
{% endif %}