{#
/**
 * @file
 * Theme override for image elements in cs-card context.
 *
 * Fixed height responsive image with center cropping.
 *
 * Available variables:
 * - attributes: HTML attributes for the img tag.
 * - style_name: The image style being used.
 */
#}
<img{{ attributes.addClass('img-fluid', 'rounded', 'cs-card-image') }}>
