{# views-view-unformatted--doctors-search--page-1.html.twig #}
{# Use Bootstrap Grid Row #}
<div class="row g-4">
  {% for row in rows %}
    {# Define the Column width here (e.g., 3 columns on large screens) #}
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      {{ row.content }}
    </div>
  {% endfor %}
</div>